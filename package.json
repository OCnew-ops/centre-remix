{
  "name": "centre-remix",
  "version": "0.1.0",
  "private": true,
  "description": "Harbour Place leasing board with honestly fake-only CALL-E fixtures (public demo never dials)",
  "scripts": {
    "dev": "npx --yes netlify-cli dev",
    "build": "echo Static site - no build step"
  },
  "dependencies": {},
  "engines": {
    "node": ">=18"
  }
}
