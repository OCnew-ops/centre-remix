{
  "name": "centre-remix",
  "version": "0.1.0",
  "private": true,
  "description": "Harbour Place leasing board with optional CALL-E dry-run wrap",
  "scripts": {
    "dev": "npx --yes netlify-cli dev",
    "build": "echo Static site - no build step"
  },
  "dependencies": {
    "@call-e/calle": "^0.1.0"
  },
  "engines": {
    "node": ">=18"
  }
}
